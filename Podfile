# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'SexyUp Social Network' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for SexyUp Social Network

  target 'SexyUp Social NetworkTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'SexyUp Social NetworkUITests' do
    # Pods for testing
  end

pod 'Firebase/Core'
pod 'Firebase/Auth'
pod 'Firebase/Database'

end
